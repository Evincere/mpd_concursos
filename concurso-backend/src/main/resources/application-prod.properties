# Configuraci贸n de MySQL para producci贸n (servidor cloud)
spring.datasource.url=jdbc:mysql://${DB_HOST:mysql}:${DB_PORT:3306}/${DB_NAME:mpd_concursos}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8
# SECURITY: Usar variables de entorno para credenciales sensibles (OBLIGATORIO en producci髇)
spring.datasource.username=${DB_USERNAME:mpd_user}
spring.datasource.password=${DB_PASSWORD:mpd_password}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JWT Configuration para producci髇
jwt.secret=${JWT_SECRET:production_jwt_secret_key_change_this_in_production_environment_minimum_256_bits_required}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Configuraci髇 CORS para producci髇 (m醩 restrictiva)
app.cors.allowed-origins=https://vps-4778464-x.dattaweb.com,https://vps-4778464-x.dattaweb.com:9003,https://149.50.132.23:8000,http://149.50.132.23:8000
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
app.cors.allowed-headers=*
app.cors.exposed-headers=Authorization,Content-Type,X-Total-Count
app.cors.max-age=7200

# Configuraci髇 de diagn髎tico de seguridad (DESHABILITADO en producci髇)
app.security.diagnostic.enabled=false

# Configuraci髇 SEGURA para el esquema de base de datos en producci髇
# PRODUCCI覰: Usar "validate" para seguridad (esquema debe existir previamente)
spring.jpa.hibernate.ddl-auto=${HIBERNATE_DDL_AUTO:validate}
# PRODUCCI覰: Deshabilitar generaci髇 autom醫ica (SEGURIDAD)
spring.jpa.generate-ddl=false
spring.jpa.show-sql=false
# Usar la misma naming strategy que en desarrollo para consistencia
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.hibernate.naming.implicit-strategy=org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl

# DESHABILITADO: Scripts SQL para evitar creaci贸n autom谩tica de usuarios de prueba
# CONFIGURABLE: Usar variable de entorno LOAD_TEST_DATA=true para habilitar datos de prueba
spring.sql.init.mode=never
# spring.sql.init.schema-locations=classpath:schema.sql  # NO usar schema.sql
# spring.sql.init.data-locations=classpath:data.sql  # DESHABILITADO

# Configuraci贸n para el almacenamiento de documentos en producci贸n
app.document.storage.location=storage

# Configuraci贸n de URL base para producci贸n
app.base-url=http://149.50.132.23:8080

# Configuraci贸n de logging para producci贸n (menos verbose)
logging.level.ar.gov.mpd.concursobackend.file=INFO
logging.level.ar.gov.mpd.concursobackend.auth.application.service.ProfileImageService=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate=WARN
logging.level.com.zaxxer.hikari=WARN

# Configuraci贸n de seguridad para producci贸n
server.error.include-stacktrace=never
server.error.include-message=never
server.error.include-binding-errors=never

# Configuraci髇 de servidor para producci髇
server.port=${SERVER_PORT:8080}
server.servlet.context-path=${CONTEXT_PATH:}

# Configuraci髇 de pool de conexiones para producci髇
spring.datasource.hikari.maximum-pool-size=${DB_POOL_SIZE:10}
spring.datasource.hikari.minimum-idle=${DB_MIN_IDLE:5}
spring.datasource.hikari.connection-timeout=${DB_CONNECTION_TIMEOUT:30000}
spring.datasource.hikari.idle-timeout=${DB_IDLE_TIMEOUT:600000}
spring.datasource.hikari.max-lifetime=${DB_MAX_LIFETIME:1800000}
# Configuraci贸n de almacenamiento base para producci贸n
app.storage.base-dir=storage
